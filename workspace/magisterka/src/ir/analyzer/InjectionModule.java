package ir.analyzer;

import com.google.inject.AbstractModule;

public class InjectionModule extends AbstractModule{

	@Override
	protected void configure() {
		//bind(Analyzer.class).toInstance(arg0);
		
	}

	
}
